import React from 'react';
import './header.scss';

const Header = () =>{

  return(
    <header class="header">
      <h1>ANAS STORE</h1>
        
    </header>
  );
};

export default Header;